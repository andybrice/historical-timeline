<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="utf-8">
  </head>
  
  <body>
    <ul>
      <% @events.each do |event| %>
        <li>
          <%= event['title'] %><br />
          
          <% if event['year'] %>
            in <%= event['year'] %>
          <% elsif event['start_year'] and event['end_year'] %>
            from <%= event['start_year'] %> to <%= event['end_year'] %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </body>
</html>